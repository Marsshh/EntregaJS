function pedinput(){
    let any = parseInt(prompt("Introduce el año:"));
    let mes = parseInt(prompt("Introduce el mes:"));
    let dia = parseInt(prompt("Introduce el dia:"));

    if(any>=0 && ){
        
    }
   
    
  
}

// Llamar a la función para obtener los valores e imprimir el resultado
pedinput();